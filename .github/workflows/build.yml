name: Build APK

# RECOVERY FREEZE - 2026-06-07
# This workflow previously ran on pushes to main and recreated the latest-debug APK release.
# It is intentionally disabled during PiBrain recovery to prevent release churn while repos are reconciled.
# Re-enable only through the recovery control queue with a specific owner, reason, and rollback plan.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    if: false # RECOVERY_FREEZE_2026_06_07
    runs-on: ubuntu-latest
    steps:
      - run: echo "PiBrainChat APK build/release is frozen during PiBrain recovery."
